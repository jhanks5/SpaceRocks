// Alignment for text
draw_set_halign(fa_center);
draw_set_valign(fa_middle);

// Draw text at element position
draw_text(x, y, "Move with arrow keys\nShoot with mouse");

// Reset alignment
draw_set_halign(fa_left);
draw_set_valign(fa_top);
